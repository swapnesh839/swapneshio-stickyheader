{
  "name": "@swapneshdeete/react-sticky-header",
  "version": "2.0.3",
  "description": "A React component that renders children within a sticky header with predefined CSS and JS.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "styles": "dist/styles.css",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/styles.css dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/swapnesh839/swapneshio-stickyheader.git"
  },
  "keywords": [
    "React",
    "React-sticky-header",
    "stickyheader",
    "stickyheader",
    "stickyheader-react",
    "header",
    "ReactHeader"
  ],
  "author": "Swapnesh",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.3",
    "copyfiles": "^2.4.1",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
